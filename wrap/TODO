- Makefile here is not generated by autotools. How should the makefile be managed.
- Should this use the headers from here? (Then it would be best to first integrate
- this into autotools, so i can use $(top_srcdir).
